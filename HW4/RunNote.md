# 遇到問題說明
Complex.cpp
1.  Complex.h 如果讀不到就使用完整路徑
2. Complex是需要編譯的編譯器如果直接幫你做好了就沒事
如果沒有就手動編譯
```
g++ main.cpp Complex.cpp -o run
./run
```
g++ main.cpp Complex.cpp -o run
把兩個檔案編譯生成出二進制檔案run

./run 執行run


